name: Build and Release

permissions:
  contents: write

on:
  workflow_dispatch: {}
  push:
    tags:
      - "v*"

jobs:
  build:
    name: ${{ matrix.build }}-build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        build: [linux, macos, windows]
        include:
          - build: linux
            os: ubuntu-latest
            rust: nightly
            target: x86_64-unknown-linux-musl
            archive-name: qt-linux.tar.gz
            # 升级到 Node.js 22 (当前 LTS) 或 24 (最新版)
            node-version: "22"
          - build: macos
            os: macos-latest
            rust: nightly
            target: x86_64-apple-darwin
            archive-name: qt-macos.tar.gz
            node-version: "22"
          - build: windows
            os: windows-latest
            rust: nightly-x86_64-msvc
            target: x86_64-pc-windows-msvc
            archive-name: qt-windows.tar.gz
            node-version: "22"
      fail-fast: false

    steps:
      # 升级到 checkout@v5 以支持 Node 24 环境
      - name: Checkout
        uses: actions/checkout@v5

      # ==========================
      # 1. 构建前端 (NPM)
      # ==========================
      # 升级到 setup-node@v5，解决路径解析和缓存问题
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
          # 确保路径相对于仓库根目录是正确的
          cache-dependency-path: plug/codesign-qt-helper/package-lock.json

      - name: Install Frontend Dependencies & Build
        working-directory: plug/codesign-qt-helper
        run: |
          npm ci
          npm run build
          echo "✅ Frontend build completed."
          ls -la dist  # 验证 dist 目录已生成

      # ==========================
      # 2. 准备 Rust 环境
      # ==========================
      - name: Install Musl Tools (Linux Only)
        if: matrix.build == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools musl-dev cmake perl
          x86_64-linux-musl-gcc --version

      # 推荐使用 dtolnay/rust-toolchain 替代 actions-rs/toolchain (后者已不再积极维护)
      # 但为了最小化改动，这里暂时保留原逻辑，如果后续报错建议更换为 dtolnay/rust-toolchain@stable
      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          profile: minimal
          override: true
          target: ${{ matrix.target }}

      # ==========================
      # 3. 构建 Rust 后端
      # ==========================
      - name: Build Rust Backend
        run: cargo build --verbose --release --target ${{ matrix.target }}

      - name: Strip Binary (Linux/Mac)
        if: matrix.build == 'linux' || matrix.build == 'macos'
        run: strip "target/${{ matrix.target }}/release/qt"

      # ==========================
      # 4. 打包归档 (Package)
      # ==========================
      - name: Package Artifacts
        shell: bash
        run: |
          mkdir -p archive
          cd archive

          # 定义二进制文件名和启动脚本
          if [ "${{ matrix.build }}" = "windows" ]; then
            BINARY="qt.exe"
            SHELL_SCRIPT="run.bat"
            cp "../target/${{ matrix.target }}/release/qt.exe" "./$BINARY"
            cp "../$SHELL_SCRIPT" ./
          else
            BINARY="qt"
            SHELL_SCRIPT="run.sh"
            cp "../target/${{ matrix.target }}/release/qt" "./$BINARY"
            cp "../$SHELL_SCRIPT" ./
            chmod +x "$BINARY" "$SHELL_SCRIPT"
          fi

          # 复制配置文件和文档
          cp ../config.toml ./
          cp ../README.md ./

          # 复制前端构建产物
          if [ -d "../plug/codesign-qt-helper/dist" ]; then
            cp -r ../plug/codesign-qt-helper/dist ./dist
            echo "✅ Copied frontend assets from plug/codesign-qt-helper/dist"
          else
            echo "❌ Error: Frontend dist directory not found!"
            exit 1
          fi

          # 创建压缩包
          # 【修复点 3】修正了之前的 tar 命令错误：之前写的是 "SHELL" (字符串)，现在使用变量 $SHELL_SCRIPT
          echo "📦 Creating archive: ${{ matrix.archive-name }}"
          tar -czf "${{ matrix.archive-name }}" "$BINARY" "$SHELL_SCRIPT" config.toml README.md dist

          echo "✅ Archive created successfully."
          ls -lh

      # 使用最新的 upload-artifact
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive-name }}
          path: archive/${{ matrix.archive-name }}
          retention-days: 5

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Download all artifacts
        uses: actions/download-artifact@v5
        with:
          path: downloaded_artifacts

      - name: List downloaded files
        run: ls -R downloaded_artifacts

      - name: Upload to GitHub Releases
        uses: softprops/action-gh-release@v2
        with:
          files: downloaded_artifacts/**/*.tar.gz
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}